<?php
    define('WF', serialize(array('WF' => 'World First')));
    define('RF', serialize(array('RF' =>'Region First')));
    define('SF', serialize(array('SF' =>'Server First')));
    define('CF', serialize(array('CF' =>'Country First')));
    define('WR', serialize(array('WR' =>'World Rank')));
    define('RR', serialize(array('RR' =>'Region Rank')));
    define('SR', serialize(array('SR' =>'Server Rank')));
    define('CR', serialize(array('CR' =>'Country Rank')));

    define('NBG',  serialize(array('NbG' => 'Number of Guilds Evaluated')));
    define('NBGD', serialize(array('NbGD' => 'Number of Guilds Downed Encounter')));
    define('NBT',  serialize(array('NbT' => 'Time after World First Encounter Clear')));
    define('NBHK', serialize(array('NbHK' => 'Number of Most Killed Encounter')));
    define('NKE',  serialize(array('NKE' => 'Number of Kills for Specific Encounter')));
    define('BEPV', serialize(array('BEPV' => 'Base Encounter Point Value')));

    define('SPREADSHEET_GLOSSARY_WORLD_FIRST',  serialize(array('World First', 'First guild to complete encounter in the world')));
    define('SPREADSHEET_GLOSSARY_REGION_FIRST', serialize(array('Region First', 'First guild to complete encounter in the region')));